#!/usr/bin/env python3
"""Executable wrapper for the status-only Codex ntfy notifier."""

from pathlib import Path
import sys


ROOT = Path(__file__).resolve().parents[1]
sys.path.insert(0, str(ROOT / "src"))

from codex_notify import main  # noqa: E402


raise SystemExit(main())
